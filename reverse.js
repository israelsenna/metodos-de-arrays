const opcoes = ['Eu', 'voce', 'ele'];
const resultado = opcoes.reverse();
console.log(resultado);
//imprime o inverso de um array
//modifica o original

const numeros = [1, 2, 3, 4, 5];
const resultadoNumero = numeros.reverse();
console.log(numeros);
console.log(resultadoNumero);
//modifica o original